# Astra 每日论文筛选与发布规范

## 范围与排序

以自动驾驶 World Models 和 VLA 为主，补充与二者直接相关的 E2E、空间/几何表征、生成式规划、闭环评测、效率与泛化工作。通常每天 3–5 篇；没有足够值得读的新论文时允许 0–2 篇，并说明检索范围。禁止为凑数重复旧论文。

先查最近 7 天，必要时扩展到 30 天并披露。按首次公开日期判断新旧；旧文新版本、录用和代码发布只能作为“已收录论文更新”，不能冒充新论文。

优先顺序综合考虑：

1. 可比协议下的强结果，优先闭环、安全分项和长尾表现。
2. 方法是否简洁、因果合理、消融充分、可迁移。
3. 工程价值：时延、内存、训练成本、数据需求、跨车泛化、维护复杂度。
4. 复现程度：实际代码、权重、配置、数据与评测入口。
5. 新颖性与时效性。不得只看榜单名次、机构名气和转发量。

## 每篇必填

- 英文原题、简称、主题、推荐等级、首次推荐日期。
- 为什么选：针对具体问题、方法洞见和证据写原创判断。
- 方法、关键结果、比较对象、数据集/划分/指标/协议；注明作者报告或独立复现。
- 一作、共同一作及对应单位。以论文首页署名映射为准，不用通讯作者单位代替一作单位。
- 时间线：arXiv v1、最新版本、已证实的录用/发表、代码或权重发布日期（未知就写未知）；注明时区。
- 发表在哪：预印本、投稿、录用、正式发表分开。查不到则写“未核实正式录用”，不得猜测。
- 一手来源：arXiv、论文正文、会议官网/OpenReview/期刊、作者项目页或官方代码库。
- 代码/权重状态：实际可用、仅占位、承诺发布、未核实分别记录。
- 局限、量产相关性（编辑判断）、最值得做的一个验证实验。

不得把预测辅助评测等同于交互闭环，不得把仿真等同于实车，不得把推理加速等同于满足车端时延。成功率改善但碰撞或舒适性退化必须同时写出。SOTA 限定到原文对比表、截至日期和协议，跨论文数值不可直接排榜。

## 去重与幂等发布

`data/papers.json` 是已收录账本；`canonical_id` 使用去掉 vN 的 arXiv ID，另记录 DOI、标题和别名。每天还要比对标题规范化结果与既有文件，防止改名、会议版和新 arXiv ID 造成同一工作的重复收录。一个工作只有一张卡片；更新版本写入原卡片的变更记录，不再作为新推荐。

每次执行先检查本地工作区与远程，干净且无分歧时 fast-forward 同步。保护他人改动，不强推、不重置、不提交无关文件。已有当日日报时继续补齐或更新，不另建重复日报。

产物：`papers/<topic>/<id>-<slug>.md`、`daily/YYYY-MM-DD.md`、`data/papers.json`、README 最新入口与归档。日报记录检索日期、窗口、查询方向、入选/未选原因和核实边界。跨主题用标签索引，不复制卡片。

提交前检查 JSON 可解析、canonical_id 与规范化标题唯一、索引中的卡片存在、本地 Markdown 链接有效、日期不在未来、必填字段齐全、`git diff --check` 无错误。仅提交本次产物，push 后核对远程分支 SHA 与本地提交相同。只有远程确认后才能说“已更新 GitHub”；失败时保留本地产物并报告具体阻塞。

日报通知简洁列出新论文、入选要点和 GitHub 链接；无新增且无实质更新时保持安静。发布失败或需要用户动作时通知。
